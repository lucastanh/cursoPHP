<?php

// is not possible see all elements inside the array through the echo, we need to pass the value index "$example[index]"

$array = [
    40,
    200,
    100,
    150,
    "school of Net",
    true,
];

echo $array[0];
echo "\n";
echo $array[1];
echo "\n";


echo $array[1];
